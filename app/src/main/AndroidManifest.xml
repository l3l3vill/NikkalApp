<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.digitalhouse.a0818moacn01_02">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/icononikkal"
        android:label="@string/app_name"
        android:roundIcon="@drawable/icononikkal"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".view.InicioActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.MainActivity"
            android:windowSoftInputMode="adjustPan"/>

        <activity android:name=".view.categorias.SugerenciaActivity" />
        <activity android:name=".view.categorias.MasEscuchado">

        </activity>
    </application>

</manifest>